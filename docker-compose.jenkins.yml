version: '3.4'

services:
  drc.vng:
    volumes:
      - ${WORKSPACE}/fixtures:/app/fixtures
  ztc.vng:
    volumes:
      - ${WORKSPACE}/fixtures:/app/fixtures

  tests:
    build:
      context: ./
    volumes:
      - ${WORKSPACE}/reports:/testenv/reports
    depends_on:
      - zrc.vng
      - drc.vng
      - ztc.vng
      - brc.vng
      - orc.vng
